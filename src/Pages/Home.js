import React from 'react';

const Home = (props) => {
    const homeSubmit = () => {
        // console.log(props.location)
    }
    return (
        <div className="home">
            <button onClick={homeSubmit}>okk</button>
        </div>
    )
}

export default Home;